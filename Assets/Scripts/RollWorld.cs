using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RollWorld : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //transform.Rotate(Vector3.up, Input.GetAxis("Vertical") * 10f * Time.deltaTime);
        //transform.Rotate(Vector3.right, Input.GetAxis("Horizontal") * 10f * Time.deltaTime);
    }
}
